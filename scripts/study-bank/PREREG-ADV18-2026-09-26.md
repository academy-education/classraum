# sat-math-v18-adv — pre-registration

Written before authoring begins, 2026-09-26, after A65/A66.

## Why this batch
Advanced Math sits at 389 with 15 per form; one medium survivor buys SAT Math
form 26 with no band decision. The commission is HARD, and after thirteen
consecutive demotions the honest expectation is medium survivors; a batch that
ships zero panel-median-hard items is reported as a failed HARD commission
whatever it buys.

## Control
Live Advanced Math, HARD band, excluding this session's cohorts
(sat-math-v17-adv, sat-math-v18-alg) AND `v2`, because A66 shows `v2` carries a
±pair tell (key in pair 87%) that would inflate any control drawn from it and
mis-describe the candidate as clean by comparison. If fewer than 14 hard
non-`v2` items are eligible, the control falls back to ANY band excluding the
same cohorts, and the fallback is stated in the register. The realised control
pool and band are printed by `make-oo-render.mjs` and copied into the entry.

## Ceiling check
Non-`v2` Advanced Math has no prior options-only number. If the control returns
above 90% the blind half is reported NOT MEASURED, not passed. If it returns
at or below its letter line the +10 bar stands as written.

## Pre-flight (all must pass on the merged file before any solver runs)
- `check-sign-pair.mjs`: zero ±pair sets, or key never in one.
- `check-key-is-composition.mjs`: zero key-as-unique-composite.
- `check-explanation-hygiene.mjs`: zero reviewer-facing text.
- `merge-halves.mjs`: no duplicate ids, keys, or closure relations.

## Bars (v17 bars, unchanged)
- Blind: candidate minus matched control <= +10.
- Per-item drop, any one: key disputed by any grader; not exclusive by majority;
  distractors weak by majority; median >= 2 of 3 distractors free-strikable;
  explanation path that does not produce its value. Unanimity is not a drop.
- EXT-1: any item a grader explicitly recommends dropping is dropped.
- HOLD: any item a grader majority calls off the published Advanced Math
  blueprint is held for Andy, not inserted.

## Need
One survivor of any band buys form 26. Zero panel-median-hard survivors is a
failed hard commission, reported as such.
