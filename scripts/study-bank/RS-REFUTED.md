# Rhetorical Synthesis: the brief family is REFUTED (2026-08-28)

Pre-registrations: EOI-V3-STAGE1-PREREGISTERED.md (original),
PHASE2-PREREGISTERED.md + addendum (rev-1, rev-2). This file is the
terminal record.

## The four tells, in order of discovery

    1. form-matches-goal        eoi-v3 stage 1     +61.1   killed
       (fixed by form symmetry — held for two shipped cohorts)
    2. element-frequency        eoi-v4, 2026-08-28  80% of decided
       consensus                (check-consensus-tell.mjs; also
                                condemned the LIVE eoi-v3 RS at 83%)
    3. minority-value inversion rev-1               57% of decided
       + self-contradicting     (pairing the wrong value made the
       twins                    un-paired true value the marker)
    4. fact-rich hosting        rev-2               hunter rule
                                verified 7/9 named predictions;
                                attack +22.2; goal-fit reading the
                                solvers' most reliable signal

## Why this is structural, not an authoring failure

The task shape puts the GOAL in the stem and requires the key to be
simultaneously (a) faithful to hidden facts and (b) the best fit to
the visible goal. Every defense against one signal re-concentrates
information in another: suppress form variance and element frequency
speaks; balance element frequency and the BALANCING APPARATUS speaks
(which option hosts the true values, which pair-member is bare).
Three revisions moved the tell; none removed it. The eoi-v3 stage-1
finding — "goal-in-stem + form-varied options is guessable by
construction" — turns out to generalize past form: goal-in-stem +
fact-corrupted options is guessable by construction at the batch
level, for any corruption scheme regular enough to author to.

## Dispositions (all executed 2026-08-28)

- rev-1 30 and rev-2 30: dead, never banked.
- LIVE eoi-v3 RS 32: ARCHIVED without replacement (verified: eoi-v3
  Transitions 27 stay live; EoI 153 → 121).
- RS authoring STOPPED. Any future attempt is a NEW design with a new
  pre-registration — e.g. selection-from-authored-variants where the
  key is chosen by seeded RNG among four independently-authored
  faithful summaries (the cr-v7 move: remove provenance rather than
  hide it), which is the only known cure for authorship asymmetry.
- check-consensus-tell.mjs and the minority-value check stay as
  standing pre-flight for ANY corruption-symmetric brief anywhere in
  the bank.

## Capacity impact

EoI 121 live (27 v3-T + 28 v4-T + 66 pre-v3). The R&W binding domain
is EoI again at ~10.6 forms. Recovering it means more TRANSITIONS
(brief proven at −38.9/−23.6, immune to all four tells) or the new
RS design above — not another revision of this one.

## Addendum: the successor design works, with one new constraint (2026-08-28)

`rsw-v1` (VOLUME-PREREGISTERED.md lane C) implemented the cure named
above — four variant note-sets per item, seeded RNG picks the shown
world after freeze. Result:

- **Guessability: cured.** Consensus checker 25% expected blind
  (chance), inverse/minority checker 0 of 11 decided, hunter found NO
  key-identifying rule after explicitly attacking value-ordering,
  position, length, hedging, goal-echo and cross-slot correlation.
  Blind attack +15.2 (bar +25) with solvers armed with all three
  historical rules.
- **New defect found: GOAL PARITY.** The design equalizes the FACTS
  across worlds but not how well each world's facts SERVE the goal.
  On 4 of 11 items the RNG keyed a rhetorically weaker variant — e.g.
  a "explain why the finding surprised researchers" item whose keyed
  world reports a result that is not surprising. A student who reads
  the goal correctly is then punished. This is a validity defect, not
  a guessability one, and the with-notes QC (3/3 on all 11) cannot see
  it because voters match facts, not goal-fit.
- Disposition: those 4 dropped, 7 banked as cohort `rsw-v1`. **Scale-up
  rule: every world must serve the goal EQUALLY — all four fact-sets
  must make their summary a genuine accomplishment of the stated goal,
  differing in the details, never in whether the goal is met.** Add a
  goal-parity reviewer to the gates (the hunter found this only by
  accident).

RS is therefore REOPENED under the symmetric-worlds design with the
goal-parity rule, and remains closed for any corruption-based brief.
