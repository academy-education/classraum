#!/usr/bin/env node
/** Ledger entry for the Advanced Math v18 kept file, bound to its exact bytes. Heredoc script, never inline. */
import { readFileSync, writeFileSync } from 'node:fs'
import { createHash } from 'node:crypto'
const D = 'scripts/study-bank'
const ledger = JSON.parse(readFileSync(`${D}/ledger.json`, 'utf8'))
const file = `${D}/sat-math-v18-adv.kept.batch.json`
const sha = createHash('sha256').update(readFileSync(file)).digest('hex')
const kept = JSON.parse(readFileSync(file, 'utf8')).length
ledger.batches.push({
  id: 'sat-math-v18-adv-kept-2026-09-27', createdAt: '2026-09-27', targetTest: 'sat', section: 'math',
  task: 'math_mc', family: 'mc_stem_source', cohort: 'sat-math-v18-adv', contentSha: sha, status: 'inserted',
  note: `14 authored as hard, ${kept} kept: panel-median 4 hard, 5 medium. The first hard maths commission in fourteen to ship a hard item. B1 dropped on the pre-registered rule (median 2 of 3 free-strikable); B2, B6, B3 and A4 dropped under EXT-1 on explicit grader recommendations (routine common-base; wrong-method key and off-blueprint by one grader; two distractor k values with no real roots contradicting the stem; strict subset of B4). Authored in three pieces after a whole-half author stalled twice, each piece audited independently, repaired once, then gated by three fresh blind solvers and three fresh with-source graders.`,
  stages: {
    shape: { passed: true, contentSha: sha, verdict: `${kept} items, all 4-choice, key present, zero ±pair sets, zero key-as-unique-composite (A5 key 4 = 2x2 shadowed by a distractor composite, printed not gated), key at an extreme 5/8 against live Advanced Math 35.6%, no reviewer-facing text. A2 options are expressions and are skipped by the numeric checkers; its closure was checked by hand by the repair agent and three graders. Six option values recur across items (4, 11, 13, 23, 25, 27), recorded not fixed as in v17/alg18.` },
    withsource: { passed: true, contentSha: sha, verdict: 'Three independent graders solved from the stem before reading explanations: ALL THREE MATCHED THE KEY ON ALL 14, every rejection step (tangency value, extraneous root, negative time, no-solution parameter) verified numerically by each, no second defensible option, every distractor path produces its printed value. Kept nine: A6 (radical, lost root plus extraneous root, sum defeats plugging), B4 (two no-solution mechanisms, sum), B5 (multiplicity before anything can be graphed) and B7 hard by panel median; A1, A2, A3, A5, A7 medium. Two graders judge B4 and B5 the only items not bypassable by Desmos or back-substitution; the third judges 13 of 14 bypassable. A1 (quartic in x²) and A5 (line tangent to circle) ruled on-blueprint borderline by two graders each, with the note that Bluebook files circle items under Geometry.' },
    nosource: { passed: true, contentSha: sha, verdict: 'Options-only attack, three samples of one solver, 14 candidates interleaved with 14 live Advanced Math items matched on the HARD band with v2 and this session cohorts excluded (52 eligible), key slots dealt flat A7 B7 C7 D7. CANDIDATE 33.3% against its own 28.6% line; LIVE CONTROL 26.2% against 28.6%. Candidate minus control +7.1, inside the pre-registered +10 bar. Unanimity 2/14 both arms. The non-v2 hard-band control sitting at its letter line is the confirmation of A66: the 69% hard-band Algebra control was v2 sign pairs, not the hard band. Pairwise solver agreement 61.9% against 27.8% independent.' },
    tells: { passed: true, contentSha: sha, verdict: 'RECORDED AGAINST A2, KEPT BY THE RULE: all three blind samples hit A2 by the same mechanism (0.331 = 1.1^3 - 1, so 2000(1.1)^5 is the designed expression), and two with-source graders independently named the same leak (1.1 printed in two options hands over the cube-root insight; the wrong method 33.1/3 ≈ 10%/month lands on the key). Panel n_struck median is 1, below the pre-registered 2, and no grader recommended dropping it, so it ships as medium; making options expressions to defeat the calculator exposed the derivation to a reader with no stem, and that is the finding. Other tells recorded: four items share the shape "two candidates, reject one by a side condition" with the rejected candidate printed (A5, A6, B1, B3; two dropped), and on A5 the side condition strikes the rejected candidate for free; the discriminant-equals-zero motor decides A1, A5, B7 (three kept) — at most two should sit in one form; A6 key is the sole even option (grader E asked to hold; rule keeps); A5/A6 use ASCII hyphen where the rest use U+2212.' },
  },
})
ledger.generatedAt = new Date().toISOString()
writeFileSync(`${D}/ledger.json`, JSON.stringify(ledger, null, 2))
console.log(`sat-math-v18-adv-kept-2026-09-27  sha ${sha.slice(0, 16)}  ${kept} items\nledger written`)
