# Volume round: six cohorts pre-registered before authoring (2026-08-28, night)

All gates are the proven ones, by reference; nothing new except lane C's
design. Authoring on Opus subagents; solvers/voters stay Haiku. Drop
never edit; payloads verified on disk; pattern hunter on every attack;
both consensus checkers on anything corruption-shaped.

## A. eoi-v5 — SAT Transitions, 60 (ids T67-T126)

Brief FROZEN verbatim from eoi-v3/v4 Transitions (measured −5.6, −23.6,
−38.9 across three attacks; immune to all four RS tells). Fresh topics.
Gates = eoi-v4's: preflight, 24-item attack + hunter (pass ≤ +25),
QC voters+graders (grader pn adjudicated by the attack per the standing
misfire note), accepts(), insert cohort eoi-v5, slot rebalance.

## B. ssat-verbal-s2 (60: SY13-42, AN13-42) and isee-verbal-s2
##    (60: IS13-42, SC13-42)

Pilot briefs plus the pilot lessons, now rules:
- SSAT synonyms: a secondary-sense stem's primary-sense trap must be an
  ASSOCIATE, never an exact synonym of the other sense; look-alike
  traps must not be near-anagrams of the stem (attack-instrument leak).
- ISEE two-blank completions: no first-blank word may repeat across
  options within an item.
- Cross-author stem collisions (the CANDID lesson): authors get
  disjoint initial-letter buckets for stems; batch-level dup check
  before gates.
Gates = the pilot gates: 3 key voters (≥2/3), blind exclusivity,
options-only attack + hunter (pass ≤ +25 at 5-slot/4-slot controls),
insert via verbal-bank-helper, cohorts ssat-verbal-s2 / isee-verbal-s2.

## C. rs-worlds-v1 — RS by symmetric worlds (RESEARCH pilot, 12 items)

RS-REFUTED.md stopped corruption-based RS. This is the named successor:
the cr-v7 move applied to RS. Each item = FOUR variant note-sets
(worlds) sharing one topic and goal, differing on 2-3 checkable facts
per world (each slot takes FOUR distinct values — element frequency is
uniform 1-1-1-1 by construction, so consensus and minority voting are
undefined). Each world gets its own goal-summary, authored
symmetrically (form symmetry retained). Only after text freeze does a
seeded RNG (base seed 20260829) pick which world's notes are SHOWN;
that world's summary is the key, the other three summaries are the
distractors. No author knows the key; blind margin expected 0 BY
CONSTRUCTION, not by discipline.
Gates: both mechanical checkers (must read ≤30% / undefined), render
check that every summary mismatches every other world on ≥1 checkable
fact (the kill-map), 12-item attack with all-rules-aware solvers +
hunter (pass ≤ +25, kill ≥ +30), QC. If the pilot passes, scale;
if it dies, RS stays closed — this is one experiment, not a revision
chain.

## D. cr-v9 — TOEFL Choose a Response, 60 quads

Third run of the cr-v7 method, brief byte-identical to cr-v8's
(topical neighborhood, indirect-competitor test, reaction-token parity
baked in). Fresh settings disjoint from the 178 live. Pre-committed
seeds: base 20260830, batch offsets +0/+100/+200 (render --seed-offset
12/112/212 on the script's 20260818 base). Gates = cr-v8's exactly,
including the frozen exclusivity brief and the no-archive ship.

## E. dl-fresh-v2 — TOEFL Daily Life, 24 fresh 2-question sets

Brief FROZEN from dl-fresh-v1 Stage 2 (flat-prior + form symmetry +
duration band + atypical fact + name hygiene with disjoint per-author
pools). Gates = dl-fresh-v1 Stage 2's: deterministic preflight,
held-out 24-question attack + hunter (≤ +25), QC voters+graders,
rationale pass, mode-A verify both directions, insert-listening cohort
dl-fresh-v2.
