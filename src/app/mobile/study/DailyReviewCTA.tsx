"use client"

/**
 * DailyReviewCTA — entry card for /mobile/study/review.
 *
 * Review is gated as coming soon, so this renders nothing (and fetches
 * nothing) until the SRS review feature launches.
 */
export function DailyReviewCTA() {
  return null
}
